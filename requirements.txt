# Runtime dependencies (tests: requirements-dev.txt).
# Pinned for Python 3.11 (PythonAnywhere "Python 3.11" system image).
# All packages ship manylinux / win_amd64 binary wheels - nothing is compiled on install.
# IMPORTANT: the .joblib model files are only guaranteed to load with the SAME
# scikit-learn / numpy versions they were trained with. Keep these pins identical
# on the machine you train on and on PythonAnywhere.
numpy==2.1.0
pandas==2.2.2
scikit-learn==1.5.2
scipy==1.14.1
joblib==1.4.2
threadpoolctl==3.5.0

Flask==3.0.3
Werkzeug==3.0.6
Jinja2==3.1.4
itsdangerous==2.2.0
click==8.1.7
MarkupSafe==3.0.2
blinker==1.9.0
Flask-Login==0.6.3
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.36
greenlet==3.1.1

# Optional - only used for the action-expiry scheduler when running self-hosted.
APScheduler==3.10.4
tzlocal==5.2
pytz==2024.2
six==1.16.0

python-dateutil==2.9.0.post0
tzdata==2024.2

